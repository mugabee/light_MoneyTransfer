{
  "name": "ledger-crm",
  "version": "1.0.0",
  "description": "CRM + live rate engine + WhatsApp integration for a crypto-fiat OTC / transfer service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
